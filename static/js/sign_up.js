const name_el = document.getElementById('fever1');
const email = document.getElementById('email');
const phone = document.getElementById('phone');
const password = document.getElementById('password');
const confirm_password = document.getElementById('correct_password');

let myObj = {
    title: name_el.value,
    text: email.value,
    phone: phone.value
}
notesObj.push(myObj);